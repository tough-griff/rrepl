#!/usr/bin/env node

const { strict: assert } = require('assert');
const { REPLServer } = require('repl');

module.exports = repl => {
  try {
    assert(repl instanceof REPLServer);
    console.log('PASS');
  } catch (err) {
    console.error('FAIL');
    console.error(err);
  }
};
