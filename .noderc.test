#!/usr/bin/env node

// assert.strict is added in v9.9.0, we run against node 8.
const assert = require('assert');
const { REPLServer } = require('repl');

module.exports = repl => {
  try {
    assert.ok(repl instanceof REPLServer);
    console.log('PASS');
  } catch (err) {
    console.error('FAIl');
    console.error(err);
  }
};
