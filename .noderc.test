#!/usr/bin/env node
const { strict: assert } = require('assert');
const { REPLServer } = require('repl');

module.exports = (repl) => {
  try {
    assert(repl instanceof REPLServer);
  } catch (err) {
    console.error(err);
    process.exit(1);
  }

  process.exit();
};
